[project]
name = "ubot"
version = "0.1.0"
description = "A minimal agent designed for u."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.24.0",
    "pydantic>=2.12.5",
    "ruff>=0.15.4",
]
